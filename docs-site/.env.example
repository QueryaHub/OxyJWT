# Optional: map the docs container to another host port (default 8001).
# OXYJWT_DOCS_PORT=8001

# Your public hostname (for reverse-proxy / DNS notes only; not read by compose).
# OXYJWT_DOCS_DOMAIN=oxyjwt.queryahub.com
